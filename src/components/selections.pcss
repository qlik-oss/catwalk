.selections {
  display: flex;
  flex-grow: 2;
  flex-shrink: 0;
  width: 70%;

  .selections-inner {
    display: flex;
    overflow: auto;
  }

  li {
    align-items: center;
    background: #fff;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    font-size: 1.5em;
    justify-content: center;
  }
  .clear-all {
    width: 6em;
    display: flex;
    justify-content: center;

    .clear {
      background: #fff;
      cursor: pointer;
      font-size: 1em;
      padding: 0;
      width: 3em;
      align-self: center;
      fill: #4d4d4d;

      &:hover {
        fill: #3f9dd0;
      }
    }
  }

  .none {
    align-self: center;
    height: 100%;
    padding: 1em;
  }
}
